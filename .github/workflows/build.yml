name: Minify CSS & JS

on:
  push:
    branches: [main]
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  minify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install minifiers
        run: |
          npm install -g clean-css-cli
          npm install -g terser

      - name: Minify CSS
        run: |
          mkdir -p dist
          cleancss -o dist/mylib.min.css src/mylib.css

      - name: Minify JS
        run: terser src/index.js -o dist/index.min.js --compress --mangle

      - name: Commit minified files
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add dist/mylib.min.css dist/index.min.js
          git diff --staged --quiet || git commit -m "chore: rebuild minified files [skip ci]"
          git push
